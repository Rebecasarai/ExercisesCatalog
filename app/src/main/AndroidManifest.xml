<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rebecasarai.github.com.exercisescatalog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FontF"></activity>
        <activity
            android:name=".Calculator"
            android:label="Calculadora" />
        <activity android:name=".Gallery" />
        <activity android:name=".MemoryGame" />
        <activity android:name=".RadioButtonCalculator" />
        <activity android:name=".ReconocimientoImagen"></activity>
    </application>

</manifest>